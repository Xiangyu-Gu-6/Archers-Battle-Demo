# 弓箭手游戏开发环境

安装与验证日期：2026-09-14。

## 已完成

| 项目 | 实际结果 |
| --- | --- |
| Godot | 4.7.2.stable.official.ed1daf0bf，Windows x64 标准版 |
| 版本校验 | 官方发布元数据 SHA256 与下载的引擎、导出模板均一致 |
| 引擎初始化 | headless 编辑器启动、空测试工程导入通过，无错误 |
| Web 模板 | 已安装 4.7.2.stable 的 8 个 Web 模板文件及 version.txt |
| Web 导出 | 空场景单线程 Release 导出成功，生成 HTML、JS、WASM、PCK 等文件 |
| MCP | @coding-solo/godot-mcp 0.1.1，社区项目，依赖按锁文件安装 |
| MCP 实测 | STDIO 初始化成功，列出 14 项工具，调用 get_godot_version 返回正确版本 |
| Codex 配置 | 已加入 godot 服务，合并后 TOML 解析通过，所有路径校验通过 |

当前只完成环境和策划。空测试工程用于检查安装，不是弓箭手 Demo；未进行游戏浏览器试玩，未发布公网网页。MCP 的场景编辑、游戏运行等工具需在后续实际工程中进一步验证。

## 启动入口与位置

双击项目根目录的 `启动Godot.cmd`，打开 Godot 项目管理器。

- 引擎：`<项目目录>\.tools\godot\Godot_v4.7.2-stable_win64.exe`
- 命令行引擎：同目录的 `Godot_v4.7.2-stable_win64_console.exe`。
- Web 模板：`%APPDATA%\Godot\export_templates\4.7.2.stable`
- MCP 包：`<项目目录>\.tools\mcp\node_modules\@coding-solo\godot-mcp`
- MCP 专用 Node：`<项目目录>\.tools\runtime\node.exe`，版本 v24.19.0，由本机已有运行时复制。
- 空测试工程：项目目录下 `.tools\verify-project`。
- 测试导出：项目目录下 `.tools\verify-web`。

`.tools` 已加入 Git 忽略列表。它包含实际安装文件，不能当作无用缓存随意删除。当前配置使用绝对路径；如果移动或改名整个项目目录，需要同步修改 MCP 配置。

## 让 Codex 加载新 MCP

配置已写入 `%USERPROFILE%\.codex\config.toml`，服务名为 `godot`。

当前任务的工具列表不会因此自动变成已验证的新列表。请在 Codex 的设置 → MCP servers 中找到 `godot`，选择 Restart；若当前任务仍未显示新工具，重新打开 Codex 后继续此任务。不要重复添加相同服务。

这是“本地连接测试已通过、配置已保存”，不是声称当前对话已经重新加载。配置和重启方式依据 [官方 MCP 文档](https://developers.openai.com/codex/mcp/)。

原配置备份位于 `%USERPROFILE%\.codex`。备份可能包含原来的私人设置，不应上传或提交到 Git。

项目内的 `tools\godot-mcp.config.toml` 保存此次新增配置片段，便于维护。没有更改现有权限策略或其他 MCP 配置。

## 重复验证

在项目目录执行：

```powershell
& '.\.tools\runtime\node.exe' '.\tools\check-godot-mcp.mjs'
```

验证结果保存为 `.tools\mcp-check.json`。测试会启动临时 MCP 服务，检查初始化、工具清单和真实引擎版本，然后关闭该测试服务。

安装包版本是 0.1.1，但该包的 MCP 握手自报版本仍为 0.1.0；这是包内版本标识差异，已经记录，不影响本次三项验证结果。

## 后续开发约定

正式工程使用 GDScript、Compatibility 渲染与单线程 Web 导出，符合 [Godot Web 导出文档](https://docs.godotengine.org/en/stable/tutorials/export/exporting_for_web.html)。导出网页需要通过 HTTP/HTTPS 服务访问，不能把双击 HTML 当成完整的浏览器运行验收。

MCP 仅用于开发，玩家访问游戏网页时不需要安装 Godot、Node 或 MCP。

来源：[Godot 官方下载](https://godotengine.org/download/windows/)、[Godot 4.7.2 官方发布](https://github.com/godotengine/godot-builds/releases/tag/4.7.2-stable)、[Coding-Solo Godot MCP](https://github.com/Coding-Solo/godot-mcp)。

