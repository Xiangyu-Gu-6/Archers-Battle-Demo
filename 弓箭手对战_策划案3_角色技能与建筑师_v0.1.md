# 弓箭手对战：策划案 3——角色专属技能与建筑师 v0.1

状态：讨论稿，未授权改动游戏代码。本文以已运行的 Demo 3 为基线；2026-09-19 已确认散弹单支 80%、鲨客主动强化射击，以及建筑师己方区域建造/占一回合/120×80 上限。其余标为“建议值 / 待确认”的内容非用户已确认规则。

## 1. 目标与现有基线

本轮让角色选择真正改变回合决策：翡翠游侠用一次散射提高覆盖面，船骸鲨客用一次重击提高单箭收益，建筑师用可被打破的实体遮挡改变射线。仍保留“看弹道、调角度、抓蓄力时机、在移动与射击中取舍”的核心体验，不加入穿透、爆炸、洞穴或永久地形破坏。

当前游戏为 2D 侧视、玩家对电脑、随机先手。双方 100 血；普通箭命中头/躯干/腿分别造成 50/30/20 点伤害。一次行动只能移动或射击；普通箭先碰到地形或角色即停止。射击预览展示试算路径前半段，上一箭的实际轨迹在下一次行动中作为淡色参考。以下新规则必须沿用同一套弹道、首碰撞与回合标识，不可仅修改视觉特效。

## 2. 待确认的核心决定

| 问题 | 本稿可执行的建议值 | 其他选择及影响 |
| --- | --- | --- |
| 散弹箭“80%”口径 | **已确认：每支箭各为普通箭的 80%**；三支全中同一部位上限为 240%，全中头部可达 120 点 | 需专项测试近距离一击击败满血角色的频率；不未经同意改为三支合计 80% |
| 鲨客“150%”触发方式 | **已确认：一次主动强化射击，占本回合行动**；发射即消耗本局一次次数 | 不做首次命中被动触发 |
| 建筑师使用次数 | 暂建议每局一次，重开恢复 | 用户只明确“前两个技能各一次”；建筑师次数仍待确认 |
| 建筑遮挡物尺寸与放置 | **已确认：己方活动区、整回合行动、最大宽 120 × 高 80 世界单位**；本稿建议额外设面积不超过 9,600 | 最小宽高、与角色间距、可放置次数仍待确认 |
| 角色选择 | 建议开局可选三名角色，玩家与 CPU 各选其一；第一版可让 CPU 固定选与玩家不同的角色 | 若只想先做“玩家选建筑师、CPU 仍是鲨客”，可缩小实现范围 |

已确认项可以作为后续实现约束；其余建议项在获得确认前只用于方案估算，不应直接写进正式平衡资源。

## 3. 共通技能规则

1. 翡翠游侠与船骸鲨客的专属射击每局各可使用一次；次数绑定**本局角色实例**，不跨局继承。建筑师次数按第 2 节待确认。
2. 选择技能只进入预览；在开始蓄力或确认建造前可以返回普通行动。**放箭或完成放置时**才消耗次数，断线/浏览器失焦按现有异常输入规则处理，不应误消耗。
3. 专属射击与普通射击共用本回合一个行动，不能先移动再使用技能，也不能技能后再射一箭。未使用技能不影响普通射击。
4. 普通箭的角度、力度、重力和飞行上限维持不变。每支技能箭也逐步使用相同弹道与碰撞顺序；不得为了特效改变实际命中点。
5. 不改变三段玩法命中区。伤害倍率在“命中部位基础伤害”之后应用，血量仍截断至 0；每支箭最多对一个角色造成一次伤害。
6. 技能 UI 必须展示名称、剩余次数与是否可用；CPU 使用技能时要有明显的角色动作、箭效和文字提示。新局清空技能状态、飞箭、遮挡物与上一局提示。

## 4. 翡翠游侠：散弹箭

### 效果与操作

- 技能名暂定“翠羽三连”。在射击选择页选“散弹箭”，之后照常调角和蓄力；松开空格同时发射三支箭，方向为当前角度 `−6° / 0° / +6°`（建议初值，按试玩调参）。三箭使用相同的本次力度与出发点附近的安全分离偏移，避免出生时互撞或误判自己。
- 三支箭各自与地形、遮挡物、敌方三段命中区做首碰撞；没有穿透、反弹或自动追踪。允许多支命中同一敌方角色，分别结算，但同一箭不能对同一目标重复伤害。
- **已确认的伤害口径：每支**造成对应命中部位普通伤害的 `0.8`，所以单支头/躯干/腿为 40/24/16；三支全中同一部位的合计分别为 120/72/48。允许各箭命中不同部位，逐支按实际命中部位独立结算；本组基准数值均为整数，无取整争议。
- 发射一刻消耗一次使用权，即使全空也不能重用。三箭全部停止、出界或超时后结束本回合；敌方在结算中归零时不再产生额外回合。
- 预览显示中央箭的前半段为主线，两侧各显示更淡的前半段；不得预报完整落点或未来部位。上一箭参考需记录三条实际路径，仍只保留“上一**次行动**”的轨迹。

### 平衡关注

散弹同时具备覆盖面与爆发：三支全中头部的理论值 120，能直接击败满血角色。这是用户确认数值的自然结果，不设置未经确认的伤害封顶或单目标命中上限。试玩必须单独记录近距离、平坡及坡面下 1/2/3 支命中率、首回合胜率和技能使用时机；若实测过强，再由用户决定是否调散射角、角色可站距离或倍率。还需覆盖三箭全空、不同部位命中、同一物理帧多箭接触建筑等情形。

## 5. 船骸鲨客：重击箭

- 技能名暂定“鲨牙重击”。主动替换本回合一次普通射击，仍是**单支**普通弹道箭，不增加速度、命中范围或穿透。
- 命中角色后，按命中部位基础伤害的 `1.5` 倍结算：头 75、躯干 45、腿 30。未命中角色、只打到地形或建筑时不造成角色伤害，但已在发射时消耗本局一次使用权。
- 对建筑的伤害仍按“一支箭 = 一次命中”计算，不因 150% 倍率直接扣两格；否则会混淆建筑的“两次伤害”规则。
- 瞄准时只显示现有前半段轨迹，不在发射前提示确定命中部位。放箭、命中反馈须明显区分普通箭，但不能让箭身视觉比实际判定范围大到误导玩家。
- CPU 只在合理命中机会下考虑使用；不能让 AI 通过隐藏的必中补偿把一次性技能变成必杀。AI 仍使用与玩家相同的瞄准误差和物理。

## 6. 新角色：建筑师与实体遮挡

### 角色定位

建筑师保留普通移动和普通射箭能力。专属行动暂定“搭建壁垒”：选择位置与形状，放置一块与程序地表贴合的实体遮挡。它改变的是**本局上方空间的碰撞物**，不是把连续地表高度数组永久挖改；重开时清除。这样既满足“改造战场”的战术意图，也不在本轮引入洞穴/任意地形布尔运算。

### 放置与限制（放置区域、行动成本与最大尺寸已确认；其他约束待确认）

- **已确认：**建筑占用整回合行动，不可与移动或射击同回合组合；只在建筑师己方活动区内的地表放置，最大宽 120、高 80 世界单位。确认放置前可撤销；确认后扣除使用次数（具体次数待确认）。
- **建议约束：**底边沿可站立地面，不能悬空、埋入地表、与角色或已有建筑重叠，也不能封死任何角色的出生安全区。
- 第一版建议近似矩形/略带顶帽的轮廓，最小宽/高各 40 世界单位，面积 `宽 × 高 ≤ 9,600`。UI 拖拽/预设尺寸都必须显示占地轮廓与合法/非法状态；最小尺寸和面积计算方法仍待确认。
- 建议同一建筑师同时最多存在一块自己的有效壁垒。若最终决定可多次建造，则还需确定“旧块保留还是替换”。
- 壁垒是**箭矢实体遮挡**：箭尖轨迹先碰到壁垒时，箭立即停止，不穿过，也不伤害壁后角色。角色是否能穿过壁垒尚待确认；第一版建议只允许贴己方安全区边缘放置，并把它视为移动障碍，以免视觉上实体却能穿人。

### 耐久与破坏

- 耐久为 **2 次有效箭矢命中**，不是 2 点角色伤害。任何普通箭、散弹中的一支或鲨客重击箭碰到壁垒，都只扣 1 次耐久。
- 第一次命中出现裂纹，仍完全阻挡；第二次命中出现破碎特效并移除碰撞。**打碎它的那支箭本身仍停下**，后续箭才能通过。若同一物理帧有多支箭同时碰到，按固定的箭 ID 顺序统一结算，避免帧率或节点遍历顺序改变结果。
- 壁垒不能由近景装饰道具代替：帐篷、旅行车和靶牌仍无碰撞。壁垒材质视觉再厚也只按定义的碰撞轮廓接触，不使用 PNG 透明轮廓作为判定。
- 破坏只清除壁垒及其视觉状态，不改变原地形坡面、角色坐标或已经停下的箭。

## 7. 角色选择、UI、AI 与镜头

建议新增轻量角色选择：显示三名角色的头像、定位、专属行动和“每局剩余次数”，开局后锁定。若用户希望保留现有快速开局，可先在调试/试玩配置中切换角色，不增加完整主菜单；最终交互待确认。

玩家行动栏加入与普通“移动 / 射击”并列的技能入口，但同一时刻只显示当前角色的一项技能。散弹和重击进入现有瞄准/蓄力流程；建筑师进入放置预览流程。不可用时显式写“本局已使用”，不应静默消失。技能提示避免遮挡角色、预测线和建造轮廓。

AI 需要识别建筑为弹道障碍：所有试射与真实箭共用壁垒碰撞；若当前路径被挡，尝试换角或移动，而非连续向墙内射击。建筑师 AI 只在有明确遮挡收益且放置合法时建造；第一轮可先保证合法与可理解，策略强度再由试玩调节。镜头跟随散弹时先以三箭包围盒中心为目标，过度分散时优先保证中央箭与敌方可见；建筑确认时展示地表接触点。

## 8. 实现边界与数据建议

- `RoleDefinition`：角色名、基础数值、技能类型、每局次数。当前普通数值先共用，不擅自给建筑师或鲨客加基础血量差异。
- `ShotDefinition`：普通 / 三箭散射 / 单箭重击，记录每支角度、速度、倍率、发射者、发射回合 ID。
- `Barrier`：放置方、碰撞多边形、耐久 2、当前裂纹阶段、局 ID。独立于连续随机地形，但与地形、角色一起参与“最早相交物”比较；预测与实箭调用同一函数。
- 用一次射击行动的统一“待结算箭数”管理三箭，不以单个 `active_arrow` 提前换回合。所有箭与壁垒都归本局容器，新局/胜负/旧异步回调清理一致。
- 伤害、次数、散射角与建筑尺寸放到可配置数据中；本稿的建议值不能散落写死在特效或 UI 脚本里。

## 9. 验收标准

1. 三角色的普通移动与射击继续成立；技能次数不会因预览、取消或失焦意外消耗，真正使用后本局不可再次选用，新局恢复。
2. 散弹恰好发出三支箭，角度和速度按配置，三条预测前半段与对应实箭开头一致；允许各自命中或被挡；同一行动只换一次回合。
3. 鲨客技能在头/躯干/腿分别造成 75/45/30，打空不造成角色伤害，撞建筑只扣一次耐久；游侠散弹每支分别造成 40/24/16，同一目标可被多支命中。
4. 壁垒超过面积/宽高、悬空、与角色重叠、越区时拒绝放置；合法壁垒与地表贴合，能阻挡预测和实箭。第一击裂纹，第二击破碎，破坏箭不穿透。
5. 建筑与三箭在同一帧碰撞时结算确定且可复现；重开 20 次不残留建筑、散弹、技能次数或旧 UI；至少 100 个地形种子检查合法放置与可射击性。
6. 1280×720 和较小窗口下，技能状态、建筑预览、三条轨迹和角色命中反馈均可读；AI 不持续对无法穿透的壁垒无效开火。

## 10. 建议实施顺序

先锁定第 2 节待确认口径；随后加入角色/技能数据与 UI 状态，再把可破坏壁垒纳入共享首碰撞，接入单箭倍率、三箭并行与回合结算，最后补 AI、镜头、美术特效和跨局清理。每一阶段独立自动测试、Web 导出并存档到 GitHub。此次交付仅为策划文档，没有修改玩法代码。
