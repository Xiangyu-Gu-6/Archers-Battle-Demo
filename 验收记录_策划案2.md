# 策划案2验收记录

版本：0.2.0  
Godot：4.7.2 stable，Compatibility，单线程 Web  
构建时间：2026-09-15 13:23:01 +08:00

## 已实现

- 每局使用独立 `MatchRoot`，重开使旧回调失效并清理地形、角色、箭、预测、上一箭和提示状态。
- 本局最多保留最近 12 支落地箭；出界和超时箭释放。
- AI、预测和实箭使用同一个弹道步进模块；玩家只看到完整试算弧长的前 50%，末段渐隐。
- 未蓄力时首发以 50% 力度预览，之后使用玩家上一箭力度；记录角度、力度、结果和停止点。
- 蓄力单程 2 秒；普通轻按 0.5°、长按 15°/秒；Shift 轻按 0.1°、长按 3°/秒。
- 移动额度 180 世界单位并按地表弧长累计；未实际位移及未蓄力瞄准可返回选择。
- 三类地形模板、25 点轮廓相似度检查、20 次共享尝试预算和多个保底形状。
- “查看敌人”、按住 Tab 临时全景、安全区角色构图、阶段文案和 1 秒命中反馈。

## 已自动验证

- `tests/smoke_test.gd`：100 个种子、头部连续碰撞、两箭爆头归零。
- `tests/v2_acceptance_test.gd`：100 个种子覆盖三种模板；180 地表移动范围不超额；预测和实际弹道逐步一致；显示路径弧长为 50%。
- 通过 UI 按钮信号链验证移动预选、返回选择、射击瞄准和返回选择。
- 连续 20 次由“再来一局”按钮信号触发新局：种子不重复、`MatchRoot` 替换、双方恢复 100 血、上一箭与落地箭状态清空。
- Godot MCP 启动工程无脚本错误；Web Release 导出成功。
- 通过本地 HTTP 在浏览器实际载入 Web 版，首回合画面与英文 UI 正常显示，浏览器控制台无警告或错误。

自动化环境禁止 Godot 写入用户级日志和编辑器设置，因此控制台会出现相关提示；进程退出码和项目测试均为 0，该提示不影响项目及 Web 产物。

## 待人工验证

- 在实际浏览器通过 HTTP 完整打完至少 3 局，并检查焦点、Tab、拖镜头和窗口缩放；当前仅完成首回合载入检查。
- 人工复核至少 10 张地图的轮廓、坡位和遮挡。
- 新版至少试玩 10 局，记录首个有效命中前射击次数、每局用时、移动次数与理由、误选撤销次数及重开残留。
- 与旧版同设备对照体验。尚未据此调整 AI 精度、弹速或伤害。
