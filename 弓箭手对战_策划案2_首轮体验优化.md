# 弓箭手对战：策划案2——首轮体验优化

日期：2026-09-15。基于 v0.1、当前 Demo 和用户试玩反馈。

本文是下一轮制作方案，不代表功能已实现。本轮只编写策划，不修改游戏代码。与 v0.1 冲突处以本文为准，其余基础规则沿用。

## 1. 本轮目标与确认范围

让玩家能够看懂弹道、逐箭修正，让移动有实际价值，并保证每次“再来一局”都得到干净的新战场。

用户已确认：轨迹显示前半段；蓄力单程放慢到 2 秒；角度调整更精细；加入上一箭参考；移动或射击仍然二选一，未移动可撤销；增加坡位和遮挡；电脑难度目前合适；地图看似未重置发生在结算后的“再来一局”，不是 F6。

本轮继续采用电脑键鼠、玩家对电脑、随机先手、100 血、头/躯干/腿脚伤害 50/30/20。技能、地形破坏、正式美术、音效、联机与菜单系统不进入本轮。

文中具体调角速度、提示时长、地图差异阈值等为策划初值，待制作试玩校准。已确认的玩法方向不因调参自行改变。

## 2. 问题依据与优先级

| 优先级 | 问题 | 当前证据 | 本轮处理 |
| --- | --- | --- | --- |
| P0 | 旧箭跨局残留 | 发射不断新增箭节点；重开只删除 active_arrow；旧箭停止后没有释放 | 统一管理本局对象，完整清理 |
| P0 | 再来一局地形看似不变 | 用户确认使用结算按钮；代码确实调用随机生成，但地形结构相似，备用地形固定 | 记录和检查实际种子、地形数据与显示，验证新地图差异 |
| P1 | 难以判断射程和弧度 | 轨迹最多前 0.2 秒、180 世界单位 | 改为预测路径前半段 |
| P1 | 蓄力与调角不易微调 | 蓄力单程 1.5 秒、调角 35°/秒 | 单程 2 秒，离散微调加持续调节 |
| P1 | 移动价值低 | 额度 80，角色躯干宽 36，约两个身位 | 统一身位口径，改为五个身位并加入坡位收益 |
| P1 | 射偏后缺少学习线索 | 没有持久的上一箭参数与落点参考 | 加入上一箭参考 |
| P1 | 战场可读性不足 | 本地观察到操作栏遮住角色下部，寻找敌人需多次拖镜头 | 战斗安全区域、全景观察与目标跳转 |
| P2 | 反馈与误操作处理不足 | 行动缺少撤回；回合文案可能保留旧状态；伤害提示短 | 明确行动提交点，整理状态提示 |

评估依据包含两个独立代码评估和主任务的本地瞄准、镜头界面观察。未完成整局实战，不据此判断 AI 命中率或先手平衡；网页标记检测器不适用于 Godot 绘制界面，零报告不作为体验通过证明。

## 3. 射击：看得懂，也保留判断空间

### 3.1 前半段预测轨迹

“前半段”定义为：用当前角度、当前力度试算，直到首次碰撞、出界或超时，然后按这段预测路径的累计弧长显示前 50%。不是固定半屏，也不是完整飞行时间的一半。

内部可以计算完整路径，但玩家界面只显示前半段：不标记未来命中部位、伤害、完整落点或后半段轨迹。轨迹最后约 20% 显示长度逐渐淡出，清楚表达后续需要判断。轨迹不得穿过地形。

发射方向、重力、步进和首碰撞判定使用与实箭一致的逻辑，避免提示线与真实飞行不符。低力度遇近处土丘时，预测线自然变短，不为满足视觉长度穿过障碍。

尚未蓄力时，首发以 50% 力度作观察预览；此后以上一次自己的发射力度作预览。界面明确显示“预览力度 XX%，按住空格开始实际蓄力”。真正按下空格仍从 0% 开始，预览随实际力度变化，避免把参考值当成已锁定发射力度。

蓄力时预测更新初值 30 次/秒，角色、角度变化时立即更新；物理照常固定步进。每次预测有步数上限，低性能浏览器优先降低预览刷新频率，不能改变物理结果或卡住蓄力条。

### 3.2 往返蓄力

- 0%→100% 用 2 秒，100%→0% 用 2 秒，完整往返 4 秒。
- 松开空格即发射，力度只影响速度，不影响伤害。
- 蓄力条显示百分比与增长/下降方向；25%、50%、75% 设置刻度。
- 蓄力开始后锁定角度。浏览器失焦取消本次蓄力并返回已锁定的瞄准状态，恢复后必须重新按空格。

### 3.3 精细调角

沿用 W/S。轻按一次变化 0.5°；长按约 0.25 秒后连续变化，速度初值 15°/秒。按住 Shift 时进入精调：轻按 0.1°，长按 3°/秒。角度范围继续为 5°—85°，显示一位小数。

输入实现区分首次按下与持续按住，不直接依赖操作系统键盘重复速度，保证不同电脑体验一致。UI 提示精简为“W/S 调角 · Shift 精调”。

### 3.4 上一箭参考

只显示玩家自己的上一箭：发射角度、力度、命中结果，以及淡色落点标记。参数在下一次射击结束后替换；没有发过箭时显示简短操作帮助。

撞地：标记真实停止点；命中角色：标记当时的命中位置并标注部位；出界/超时：显示方向和“出界/超时”，不虚构地面落点。目标或玩家移动后，提示“站位已变化，参考为上次射击”，不自动修正旧参考。

标记区别于当前预测线和实箭，不保留完整历史轨迹。玩家移动不会删除上一箭记录；开始新局则全部清空。参数只供观察，不提供一键复射或自动代替玩家松手。

## 4. 移动：五个身位，明确收益和代价

### 4.1 距离与撤销

本轮将一个身位定义为当前占位角色躯干宽 36 世界单位，移动额度为 180 世界单位，即五个身位。未来更换美术后使用独立 gameplay_body_width 参数，避免贴图边缘改变玩法数值。

移动与射击仍然二选一。点“移动”进入预选：显示可达地面区间和“返回选择”，在角色尚未发生实际位移前允许撤销。真正走出第一段有效距离后锁定移动行动，不能改为射击；遇边界但没有位移不算提交。数值抖动不算位移，用微小容差判断。

移动额度按沿地面的累计路程扣除，往返均扣费，不是只计算起点到终点距离。地形坡度、区域边界会缩短实际可达区间，界面应显示真实范围，而不是承诺任何位置都能走满五身位。

保留 A/D 移动和“结束移动”；额度用尽自动结束。移动速度暂保留 125 世界单位/秒，以沿地面路程计，走满约 1.44 秒。

射击模式在未开始蓄力前也允许返回行动选择。一旦开始过蓄力，即使失焦导致蓄力重置，也不允许改选移动。

### 4.2 地形提供小范围策略

保留连续地表与两侧活动区，在每侧可行走区域加入缓坡、浅凹位、局部凸起。目标是让一次移动可以改变高度或减少部分遮挡，不需要加入跳跃、洞穴或独立掩体系统。

地形初值：单次移动可获得约 20—50 世界单位的高度变化；局部土丘可以遮住部分腿脚或躯干，但不能让角色完全无解。出生点至少一侧具有接近完整移动额度的通路，并保证有一条可行射击路线。

优先制作三类生成模板：低中央丘陵、左右不等高坡地、带浅凹位的错落坡地。模板内再随机坡长、丘位、高度与出生位置，使相邻对局轮廓有可见差异。

角色两侧使用同样移动额度。电脑维持现有难度与瞄准误差，不主动增强命中能力；只修正因新坡地导致的卡住、越界或持续无效移动。若更长移动和地形变化间接改变难度，先记录试玩结果，不暗中加强 AI。

## 5. 新局生命周期：彻底清理后再生成

### 5.1 对“再来一局”的判断

结算按钮本来就请求新种子，因此不能把用户报告归因于 F6。实施时先记录连续重开的实际种子、最终地形高度数组摘要、模板类型、备用触发情况和箭节点数，同时核对正在试玩的包是否对应当前源码。

这样区分四种情况：种子未变、种子变但最终地形一致、地形不同但视觉过于相似、旧显示或旧构建包未更新。旧箭未清理已从代码确认；地图具体原因仍需通过上述运行证据确定。

### 5.2 生命周期顺序

1. 收到重开请求后立即禁用重复点击，进入生成状态。
2. 增加局标识和回合标识，让旧 AI、计时器、伤害与镜头回调失效。
3. 清空按键状态、拖镜头状态、蓄力、预测线、上一箭参数和标记、命中文字与结算层。
4. 删除统一 MatchRoot 下的旧地图、双方角色、飞行中与已落地箭、临时效果；等待节点清理完成，再建立新局对象。
5. 生成不同于上一局的随机种子，创建并校验新地形。
6. 放置新角色，恢复生命、45°初始角度、移动额度与默认镜头；重新随机先手。
7. 展示新地图，开放新局操作。

永久 UI 与局管理器可以复用，但其显示状态必须按新局重置。所有箭统一属于本局容器，不能仅依赖一个 active_arrow 引用做清理。本局中可保留最近 12 支落地箭用于战场痕迹，超出后释放最早的；独立的“上一箭参考”不依赖这些节点存在。

### 5.3 新地图差异与兜底

新种子与上一局不同只是第一层检查。生成结果还要比较上局轮廓：先排除完全一致的高度数组，再检查丘陵位置、出生区高度差与地表采样差异。优先切换地形模板。

相似度初值：等距采样 25 点，平均高度差不足 15 且主要丘陵位置差不足 120 世界单位时，视为过于相似并重新取样。该阈值需要通过可见轮廓复核，不能只为通过指标制造不可玩的地图。

生成与差异检查共享最多 20 次尝试预算，避免嵌套重试卡住页面。失败时从多个预验证备用地形中选择与上一局不同且可玩的一个，不再永远返回单一固定备用形状。

普通“再来一局”始终换地图。F6 同种子复测只保留在调试构建，发布试玩版不暴露这个入口；调试同种子也必须彻底清理旧对象。

## 6. 战场可读性与反馈

### 6.1 镜头与操作栏

将顶部状态栏和底部操作栏排除在战斗安全显示区域之外。镜头聚焦角色时，头、脚、弓和脚下地表完整位于安全区；箭命中时，命中位置也应避开栏位。

加入“查看敌人”和按住 Tab 临时查看全图，松开返回此前镜头位置。已有“回到角色”保留。非 UI 区拖动镜头时显示一次性简短提示；界面新增功能按当前状态显示，避免同时堆满按钮。

全景和查看敌人在己方选择、移动预选、瞄准阶段可用。蓄力开始时返回射手构图，暂不允许视图切换，让玩家专注力度。发射后跟箭，命中短暂停留再进入下一回合。

### 6.2 结果与状态文案

命中部位、伤害提示约保留 1 秒，最后一箭结果可在上一箭参考区域继续查看。换回合必须刷新主提示，避免“玩家行动”下仍显示“电脑正在瞄准”等过期文字。

采用明确阶段提示：请选择行动 → 移动预选/射击瞄准 → 行动已提交 → 蓄力/飞行 → 命中结算。移动未提交、已提交采用不同按钮状态，不能只靠一段说明区分。

地图种子、生成次数与备用状态移入调试显示，普通玩家界面不占位。反馈先用简短文本、血条变化和已有几何表现实现，不引入正式音效、复杂慢动作或强烈镜头震动。

## 7. 核心参数对照

| 项目 | 当前 | 本轮方案 |
| --- | --- | --- |
| 轨迹预览 | 前0.2秒且不超过180单位 | 首碰撞/出界/超时前路径弧长的前50%，末端渐隐 |
| 往返蓄力 | 单程1.5秒 | 单程2秒，周期4秒 |
| 普通调角 | 35°/秒 | 轻按0.5°，长按15°/秒 |
| 精细调角 | 无 | Shift：轻按0.1°，长按3°/秒 |
| 移动额度 | 80单位 | 180单位，五身位 |
| 未移动撤销 | 无 | 可以返回行动选择 |
| 上一箭参考 | 无 | 角度、力度、结果与淡色落点 |
| 命中反馈停留 | 0.6秒 | 初值1秒 |
| 重开清理 | 仅最后一支箭等部分状态 | 按局清理所有对象和状态 |
| 地形变化 | 单一构型随机参数，单备用 | 多模板、相似度检查、多备用 |
| AI难度 | 当前单难度 | 保留，不增加瞄准精度 |

## 8. 制作步骤、依赖与交付

| 顺序 | 工作 | 主要影响模块 | 验收后交付 |
| --- | --- | --- | --- |
| 1 | 复现结算重开，记录种子、地形摘要与对象数；核对源码和试玩包 | main、terrain、构建信息 | 重开问题的明确运行证据 |
| 2 | 建立本局容器与完整重置；失效旧回调；清理历史箭和预测 | main、arrow | 任意时刻重开不残留、不串回合 |
| 3 | 抽出共享弹道试算，实现前半段预览与上一箭记录 | main、arrow、弹道模块 | 可观察、可校准的射击反馈 |
| 4 | 调整蓄力、微调角度、未提交行动撤销 | balance、main、输入配置 | 完整键鼠操作闭环 |
| 5 | 五身位移动、地面可达范围、坡地路程扣费 | balance、archer、main | 可见范围与真实移动一致 |
| 6 | 多种坡地模板、遮挡与新局差异校验 | terrain、AI合法性检查 | 有变化且可玩的随机地图 |
| 7 | 战斗安全区、全景和敌方镜头、状态文案与反馈 | camera、UI | 小窗口下角色和关键提示不被遮挡 |
| 8 | 集成验证、重新导出Web、对照试玩 | tests、export | 可试玩版本、验收记录与调参记录 |

实现时优先修复重开，再优化射击，再扩展移动与地形，最后整合界面。不要同时改弹速、伤害和 AI 精度，以免无法判断体验改善来自哪里。

## 9. 验收标准

### 功能与回归

- 至少连续20次从真实结算按钮进入新局；每次都恢复双方100血、随机先手，种子不同、无旧箭/旧轨迹/旧参考/旧伤害提示。
- 完整打过至少3局再检查箭与临时对象数量，不能跨局增长。测试重复点击重开和旧AI回调延迟到达，不得重复生成或伤害新角色。
- 自动化另覆盖蓄力、飞箭、结算等待中的重置，并保留至少一次真实UI点击链验证，不能只调用内部函数替代按钮验收。
- 预测前半段与实际箭路径在相同参数下吻合，重力和遮挡一致；不展示后半段或未来落点。高抛、低力、近丘遮挡、出界都需覆盖。
- 蓄力单程2秒、周期4秒；轻按微调不受帧率或系统键盘重复率影响；失焦、按键重复不会误射。
- 未移动时可撤销；实际移动后不可改射击；180单位累计地表路程扣完即结束；边界截断范围正确。
- 上一箭参考只记录已发生的射击；移动后提示参考条件变化；新局全部清空。
- 自动生成至少100个种子，检查出生可站立、坡面可行走、预览与实箭碰撞一致、双方合法射击路线与有限重试。人工检查至少10张图的遮挡和轮廓差异。
- 在实际出生位置及活动区采样组合检查双向弹道可达性，不仅检查地图中点；排除完全安全的无解躲藏点。
- 1280×720与较小窗口下，角色全身、附近地面和核心按钮可见；全景退出恢复原镜头，UI点击不拖动战场。
- 重新构建Web并记录构建时间/版本标识，确认浏览器载入新资源；通过HTTP服务验证完整对局，避免把旧构建或缓存表现误认为代码结果。

### 体验验证

以相同设备分别试玩旧版与新版，再收集玩家感受：是否更容易理解弧线、能否根据上一箭修正、调角是否过头、移动是否改变下一箭选择、换图是否明显。

记录首个有效命中前的射击次数、每局用时、移动次数及理由、误选撤销次数、重开残留次数。先完成至少10局新版试玩观察；这只用于发现问题，不作为最终胜率或数值平衡结论。

通过标准：重开残留为0；玩家能够解释上一箭失误并有目的地调整下一箭；至少部分地图上移动能带来可感知的高度或遮挡变化；操作辅助没有变成自动命中。若AI体验改变，先核对地形与辅助影响，再与用户讨论是否调整难度。

## 10. 本轮完成后的交付

交付Godot源码、重新导出的Web试玩包、改动说明、测试记录及已知问题。每一项标注已实现/已验证/待验证，不能用本策划替代实际结果。

本轮完成后再讨论技能与特性。目标是先建立可学习、可判断、可稳定重开的基础对战体验。

