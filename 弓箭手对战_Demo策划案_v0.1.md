# 弓箭手对战 Demo 策划案 v0.1

日期：2026-09-14。状态：需求已确认，供制作执行与试玩调参使用；本文不是已经完成的游戏功能清单。

## 1. 游戏定位与目标

一款在电脑浏览器运行的 2D 侧视、回合制弓箭对战游戏。玩家与一名电脑角色分居地图两侧，观察随机起伏地形，在“调整位置”与“立即射击”之间取舍，通过射击角度和往返蓄力条控制箭的抛物线，命中敌人不同身体部位造成伤害。

Demo 要验证三件事：射箭是否有学习和进步空间；移动是否值得消耗一次射击机会；随机地形是否带来不同打法而不制造无解对局。

使用 Godot、GDScript、Compatibility 渲染模式，目标是单线程 Web 导出。只支持电脑键鼠、玩家对电脑；采用几何图形占位，确保头、躯干、腿脚清晰可辨。后续正式美术另行讨论。

## 2. 范围

本版包含：随机地图、随机先手、双角色血量、移动与射击互斥、角度调整、往返蓄力、短轨迹提示、部位伤害、单难度 AI、镜头观察与跟箭、胜负结算、再来一局，以及浏览器运行验证。

本版不包含：正式主菜单、音效音乐、角色选择、技能实际效果、地形破坏、跳跃、风力、穿透、反弹、道具、复杂地形、联机、本地双人、存档、账号或商业化。

后续确定要支持的方向：不同血量的角色、特定部位免伤、爆炸箭、地形破坏。具体角色和技能数值待下一阶段确定。

## 3. 完整对局流程

1. 生成地图种子，构建地面并检查出生区与弹道可达性；校验失败时重新生成。
2. 左侧放置玩家、右侧放置电脑，各 100 血。随机选择先手，双方概率相同。
3. 短暂展示全地图，随后镜头转向先手。展示期间不接受战斗操作，可点击跳过。
4. 当前角色选择移动或射击，执行一个行动。
5. 行动完成后结算；若任意角色血量为 0，立即结束对局，否则交换行动方。
6. 结算层显示胜负、双方剩余血量和“再来一局”。重开使用新种子，重新随机先手。

“回合”在本文指一方的一次行动。没有操作倒计时，不设置最大回合数或强制平局。调试功能可额外提供同种子重开，但不占正式操作区域。

## 4. 行动与移动

回合开始显示“移动”“射击”两个按钮。进入射击瞄准后，在尚未开始蓄力时可以返回选择；确认移动或开始蓄力即锁定本回合行动。

选择移动后，A/D 左右移动，只能在自己一侧标记的活动区域内行动。不能跳跃或越过区域边界。每回合移动额度约为两个角色身宽，按沿地表实际行走的累计距离扣除，往返也消耗额度。

移动额度耗尽或点击“结束移动”后换人。可以提前结束，也可以不位移就结束，但仍消耗本回合。撞到边界时只停止移动、不自动扣剩余额度，仍可转向或结束。没有合法移动空间时禁用移动按钮并说明原因。

角色沿可通行斜坡贴地移动，不能穿地、悬空或跨越障碍。地图生成必须保证活动区有可站立空间，坡度满足角色行走能力。

设计意图：移动用于离开对手已校准的位置、调整高度或寻找较少遮挡的发射点。双方共享规则，AI 移动也消耗整回合。

## 5. 射击操作

### 键鼠

| 操作 | 输入与反馈 |
| --- | --- |
| 选择行动 | 点击移动或射击 |
| 移动 | A/D，仅移动行动中生效 |
| 调整仰角 | W/S，上下调整，显示当前角度 |
| 蓄力和发射 | 按住空格开始蓄力，松开发射 |
| 观察地图 | 鼠标左键拖动非 UI 区域，仅自己的行动阶段 |
| 返回当前角色 | 点击“回到角色” |
| 结束移动 | 点击“结束移动” |

角色默认朝向敌人，仰角相对朝敌方向的水平线计算。初始暂定 45°，范围 5°—85°，角度调节速度暂定 35°/秒。角度在蓄力开始时锁定，下一回合保留上次使用的角度。

### 往返蓄力

力度从 0% 线性上升到 100%，再下降到 0%，循环往返；单程暂定 1.5 秒，一个完整周期 3 秒。松开空格按当时力度发射一次，之后忽略持续按键和重复释放事件。

0% 也对应最低初速度，避免出现零速度箭。暂定初速度 250—1450 像素/秒，力度只控制初速度，不改变伤害。重力暂定 700 像素/秒²。所有数值都是第一轮调参起点，不代表最终平衡。

蓄力过程中实时展示开头一小段预测轨迹，长度同时受 0.20 秒预测时长和 180 像素弧长限制，以先达到者为准；遇地形即截断。未蓄力时用最低力度显示短轨迹。不给完整落点，也不永久保留上一箭的完整轨迹。

每次松开都发射，不提供常规取消蓄力来规避时机判断。浏览器失焦属于异常：暂停战斗并清除按键状态，恢复时回到已锁定的射击瞄准状态，力度重置；不误射、不额外消耗回合。恢复需新的空格按下事件。

### 弹道与碰撞

箭受重力运动，无风阻、风力和反弹。箭头朝向当前速度方向；碰到地形停下，碰到角色执行一次命中结算后停止。发射时箭尖位于弓前，基础版忽略发射者自身碰撞。

弹道预测和实际飞行使用同一套运动计算。每个物理步检测上一位置到下一位置的线段，取最先相交对象，避免高速箭穿过头部或土丘。角色伤害以箭尖轨迹首次接触的身体区域为准，尾羽不触发伤害。

射击在命中、撞地、越过世界边界，或飞行超过 8 秒时结束。飞出屏幕不等于飞出地图，不能因为镜头不可见提前销毁箭。结算完成后才允许换人，确保一回合只结算一次。

## 6. 生命值、部位与胜负

| 部位 | 单次伤害 | 满血仅命中该部位的致胜箭数 |
| --- | ---: | ---: |
| 头部 | 50 | 2 |
| 躯干（含手臂） | 30 | 4 |
| 腿脚 | 20 | 5 |

双方初始及最大血量均为 100。扣血后使用 max(0, 当前血量 − 伤害)，血条和数字最低均为 0。立即检查胜负，败方不得获得额外行动。

头、躯干、腿脚碰撞区不重叠；视觉与判定位置一致。相邻区域边界使用固定归属规则，不能一箭重复命中多个部位。命中反馈显示“头部 −50”等文字并更新血条。无暴击概率、流血、护甲、伤害浮动、距离衰减和击退。

两箭爆头意味着精准射击收益高，因此头部要明显小于躯干，随机先手不能代替后续先手胜率评估。初期试玩目标暂定每局约 2—5 分钟，只作调参参考，不通过倒计时强制实现。

## 7. 随机地图

暂定基准视口 1280×720，世界宽约 2400 像素，角色占位宽约 40、高约 100 像素。左右活动区分别位于地图约 8%—28%、72%—92% 范围，具体边界随合法地形确定。单回合移动上限初值 80 像素。

地图为连续地表，以有限控制点生成平缓坡面，在中间加入少量凸起土丘，地形填充至世界底部。允许两边高低不同、遮挡不同，但不生成洞穴、悬空平台、深坑或出生点被封闭的结构。高空区域预留足够空间容纳抛物线。

生成分为：种子取样 → 地面高度生成 → 坡度限制 → 出生区整形 → 碰撞体构建 → 可玩性校验。

校验必须覆盖：

- 双方不重叠、不埋入地面，脚底落在合法地表上。
- 活动区连通，存在至少一个有实际位移价值的方向。
- 土丘不能封死所有射击路径。出生点双向至少有一组合法角度、力度可到达敌方身体，使用与实际箭相同的碰撞模拟验证。
- 双方活动区内的可站立采样位置不能出现明显的完全无敌点。检查双方位置组合的可达性，发现不合理遮挡就降低土丘或重生成。
- 连续失败最多 20 次后使用预验证的备用地形，避免无限生成。

随机目标是变化而非任意拼接。种子、出生点和校验结果可写入调试日志，用于复现错误。复杂地形和完全可破坏地形在后续独立迭代。

## 8. 电脑 AI

本版只有一个普通难度。AI 使用双方当前位置、当前地形、同样的角度和力度上限规划动作，不修改箭的飞行轨迹，不自动追踪移动后的目标。

射击时先用有限候选角度与力度进行弹道试算，找出可命中或最接近目标的方案，再对角度与力度加入误差。初始误差范围暂定角度 ±2°、力度 ±3 个百分点；通常瞄准躯干，约 20% 的射击尝试头部。实际命中率通过随机地图试玩再调整，不承诺每次命中。

AI 可根据上一箭偏短或偏长微调自己的系统偏差，但保留随机误差下限；对手移动后降低旧校准结果的参考权重。不得逐回合最终变为必中特效。

有可用射击路径时优先射击；若附近位置能明显改善遮挡或射击角度，可选择移动。避免连续两回合移动，避免无意义左右摇摆。移动候选仅在本回合距离额度和己方活动区内。

AI 的最终力度通过同样的往返蓄力逻辑和释放机制执行，稍作观察与瞄准演示。弹道搜索需分帧或设候选上限，避免浏览器单线程停顿。

## 9. 镜头与必要界面

开局缩放展示地图，随后切向当前角色。玩家回合可拖动查看敌人和阻挡；镜头受世界边界约束。“回到角色”按钮始终可用。箭发射后自动跟随箭，命中短暂停留约 0.6 秒，再平滑切向下一个角色。

固定界面显示：双方姓名与血量、当前行动方、移动/射击按钮、角度、往返力度条、剩余移动额度、简短键位说明。观察敌人时仍可看到角度和力度条，不要求瞄准时同时看见双方。

行动按钮有明确不可用状态。AI 行动和箭飞行时禁用玩家战斗输入。首次打开页面需点击游戏区域取得键盘焦点，空格不能导致网页滚动。UI 点击不得同时触发镜头拖动。窗口缩放后血条与主要按钮不得被裁切。

占位画面只强调可读性：两方颜色不同，头部明确，地形边缘清楚，箭与短轨迹对比明显。不在 Demo 阶段确定正式美术风格。

## 10. Godot 实现划分与后续扩展

| 模块 | 本版职责 | 后续扩展方向 |
| --- | --- | --- |
| 对局与回合管理 | 先手、行动锁定、状态切换、胜负 | 技能行动、额外回合效果 |
| 角色与移动 | 贴地、行动额度、血量和部位区域 | 不同基础血量与移动能力 |
| 射击控制 | 角度、蓄力、生成发射参数 | 不同武器和蓄力方式 |
| 弹道与箭 | 统一运动计算、首碰撞与停止 | 爆炸箭、穿透等独立行为 |
| 伤害结算 | 命中事件、部位伤害、扣血、死亡 | 躯干免伤、减伤、范围伤害 |
| 地形 | 种子、地形数据、碰撞重建与校验 | 局部破坏、重建与落地处理 |
| AI | 选择行动、试算与误差 | 角色技能策略和多个难度 |
| 镜头与 UI | 观察、跟箭、状态与输入反馈 | 正式表现与教学 |

推荐显式状态流程：生成 → 开局展示 → 行动选择 → 移动，或瞄准 → 蓄力 → 箭飞行 → 结算 → 下一回合 / 结束。输入和异步回调必须核对当前状态与回合标识。

角色数值、武器数值、AI 参数使用 Godot Resource 数据配置。箭提供命中事件，伤害模块根据部位和目标特性结算，UI 只展示结果。不要把“头部 50”写进箭的移动代码。

地形保留可修改的数据表示，绘制与碰撞从同一数据生成。本版可以使用地表折线/多边形，但复杂爆炸坑和洞穴可能需要升级为分块多边形或其他表示；预留接口不等于已经解决任意地形破坏。

后续技能迭代建议顺序：差异血量 → 部位免伤 → 爆炸范围伤害 → 地形破坏。届时另行确定技能次数、冷却、是否占用行动、爆炸遮挡、范围伤害是否分部位、坍塌后角色如何落地、同时死亡如何判定。

## 11. 制作步骤与每步验收

按依赖顺序推进，每一步先达到验收条件再叠加下一层。

| 步骤 | 要做的工作 | 交付与验收 |
| --- | --- | --- |
| 0 开发环境 | 安装 Godot 稳定版与对应 Web 模板，配置 Godot MCP，记录版本和启动路径 | Godot 能启动；MCP 握手、列工具、读取版本成功；Web 模板就位。当前会话未重新加载的情况须单独说明 |
| 1 最小工程 | 建 Godot 工程，设置 GDScript、Compatibility、基准分辨率与键位；用临时场景做 Web 导出 | 浏览器经本地 HTTP 打开导出页，显示测试场景；尽早验证 Web 路线 |
| 2 固定地形与角色 | 暂用固定坡地，放两个占位角色，分开身体碰撞与三个伤害区 | 双方站立稳定；斜坡不穿地；可视部位与命中区一致 |
| 3 单箭射击 | 完成调角、往返蓄力、短轨迹、飞行、首碰撞、出界与超时 | 相同参数弹道一致；短轨迹与实箭开头吻合；高速箭不穿过小目标 |
| 4 战斗闭环 | 加回合状态、随机先手、100 血与三档伤害、胜负重开；AI 暂用简单合法射击 | 可从开局打到结算；头部两箭致胜；死亡血量归零；无双重回合或重复伤害 |
| 5 移动取舍 | 加移动按钮、己方活动区、累计距离、手动结束与行动锁定 | 同回合不能移动又射击；往返扣额度；边界、坡面和零移动结束正确 |
| 6 随机地形 | 将固定地图替换为种子生成、坡度限制、出生校验与弹道可达性检查 | 自动跑至少 100 个种子；无无效出生和生成死循环；人工复核高低差与遮挡极端地图 |
| 7 普通 AI | 加弹道候选搜索、误差、历史修正及移动决策 | AI 使用同样规则，有命中也有失误；无持续无效移动；思考不冻结浏览器 |
| 8 镜头和反馈 | 完成全图展示、拖动、回到角色、跟箭、部位提示和键位说明 | 双方不在同屏时仍可观察和射击；UI 不与拖动冲突；命中反馈可读 |
| 9 Web 整体验收 | 导出完整 Demo，浏览器测试重开、焦点、按键、缩放、长抛物线和持续对局 | Chrome/Edge 验证完整流程，Firefox 做兼容性复核；记录浏览器版本与实际结果 |
| 10 试玩调参 | 玩至少 10 局，记录时长、命中部位、移动次数、先手胜负和卡点 | 根据体验调地图、弹速、误差和蓄力速度；保留修改记录。10 局用于初步发现问题，不作为统计平衡结论 |

阶段里程碑：步骤 1 验证浏览器路线；步骤 4 能完整打一局；步骤 7 核心玩法完整；步骤 10 交付可试玩 Demo。

## 12. 必须覆盖的边界情况

- 30 血受头部伤害后显示 0；100 血连续两次头部命中即结束。
- 同物理步穿过多个区域只取最先碰撞；地形先于角色被碰到时不伤害身后角色。
- 箭刚出弓不会误伤自己；高抛离开视口仍正常飞行。
- 空格按键重复、按住切换回合、蓄力时失焦，不会额外发箭。
- 连续点击行动和重开不会生成多个箭、AI 或地图。
- 移动途中撞边界、反向移动、提前结束，额度与回合始终一致。
- 新局清理上局箭、伤害提示、AI 任务和延迟镜头回调。
- 随机地图失败有有限重试和备用地图；AI 没有精确解时仍会完成合法行动。
- 页面隐藏时暂停；恢复后输入状态干净，力度不瞬跳，物理不因累计巨大时间步穿透。

## 13. 最终交付与发布路径

Demo 交付包括 Godot 工程源码、固定版本说明、Web 导出目录、本地启动说明、数值配置、已知问题和验收记录。玩家打开网页即可玩，不需要安装 Godot 或 MCP；MCP 只服务开发工作。

成型后再确定公网托管位置，将完整 Web 导出文件部署到支持相应文件类型的 HTTPS 静态托管服务，并验证公开链接。此阶段不创建账号、不购买域名，也不把策划阶段当成已获准发布游戏。

## 14. 技术参考

- [Godot Windows 官方下载](https://godotengine.org/download/windows/)：标准版为便携解压运行方式。
- [Godot Web 导出文档](https://docs.godotengine.org/en/stable/tutorials/export/exporting_for_web.html)：采用 GDScript、Compatibility 和单线程 Web 导出；通过服务器访问导出文件。
- [Coding-Solo Godot MCP](https://github.com/Coding-Solo/godot-mcp)：社区 MCP 开发工具，用于版本读取、项目运行和调试等。
- [Codex MCP 配置文档](https://developers.openai.com/codex/mcp/)：STDIO 服务命令、参数、环境变量与配置文件方式。

实际安装版本、连接验证结果与限制另见《开发环境说明》，不以在线页面版本代替本机验收结果。

